$CLOCK_FONT = Inter Variable
$FONT = Inter Variable
$MONOSPACE_FONT = CaskaydiaCove Nerd Font Propo

animations {
  animation = fade, 0
}

background {
  path = $BACKGROUND_PATH
  color = $primary_container
  blur_passes = 1
  blur_size = 0
  brightness = 0.65
}

# --- Shadow effect --- #

shape {
  size = 100%, 1
  color = rgb(000000)
  shadow_passes = 4
  shadow_size = 12
  shadow_color = $shadow
  shadow_boost = 1.5
  position = 0, 0
  halign = center
  valign = top
  zindex = 0
}

shape {
  size = 100%, 1
  color = rgb(000000)
  shadow_passes = 4
  shadow_size = 12
  shadow_color = $shadow
  shadow_boost = 5.0
  position = 0, 0
  halign = center
  valign = bottom
  zindex = 0
}

# --- Main box --- #

shape {
  size = 800,500
  color = $surface
  border_size = 2
  border_color = $surface_variant
  shadow_passes = 2
  shadow_size = 5
  shadow_color = $shadow
  halign = center
  valign = center
  rounding = 20
  zindex = 1
}

# --- Container boxes --- #

shape {
  size = 160,460
  color = $surface_container
  border_size = 2
  border_color = $surface_container_high
  halign = center
  valign = center
  position = -300, 0
  rounding = 10
  zindex = 2
}

shape {
  size = 160,460
  color = $surface_container
  border_size = 2
  border_color = $surface_container_high
  halign = center
  valign = center
  position = 300, 0
  rounding = 10
  zindex = 2
}

shape {
  size = 400,150
  color = $surface_container
  border_size = 2
  border_color = $surface_container_high
  halign = center
  valign = center
  position = 0, 155
  rounding = 10
  zindex = 2
}

shape {
  size = 400,120
  color = $primary
  border_size = 2
  border_color = $primary
  halign = center
  valign = center
  position = 0, 0
  rounding = 10
  zindex = 2
}

shape {
  size = 400,150
  color = $surface_container
  border_size = 2
  border_color = $surface_container_high
  halign = center
  valign = center
  position = 0, -155
  rounding = 10
  zindex = 2
}

# --- Screen Borders & Outlines --- #

shape {
  size = 100%, 8
  color = $surface
  halign = center
  valign = top
  zindex = 2
}

shape {
  size = 100%, 8
  color = $surface
  halign = center
  valign = bottom
  zindex = 2
}

shape {
  size = 8, 100%
  color = $surface
  halign = left
  valign = center
  zindex = 2
}

shape {
  size = 8, 100%
  color = $surface
  halign = right
  valign = center
  zindex = 2
}

shape {
  size = 100%, 10
  color = $on_primary_fixed_variant
  halign = center
  valign = top
  zindex = 1
}

shape {
  size = 100%, 10
  color = $on_primary_fixed_variant
  halign = center
  valign = bottom
  zindex = 1
}

shape {
  size = 10, 100%
  color = $on_primary_fixed_variant
  halign = left
  valign = center
  zindex = 1
}

shape {
  size = 10, 100%
  color = $on_primary_fixed_variant
  halign = right
  valign = center
  zindex = 1
}

# --- Clock seperator --- #

shape {
  size = 130, 4
  color = $outline_variant
  position = 300, 0
  halign = center
  valign = center
  zindex = 3
}

# --- User image --- #

image {
  path = $PROFILE_IMAGE
  reload_cmd = $SCRIPTS_PATH/user-picture.sh
  reload_time = 0
  border_size = 0
  border_color = $primary
  size = 100
  halign = center
  valign = center
  position = -120, 155
  rounding = -1
  zindex = 3
}

# --- User & Uptime labels --- #

label {
  color = $on_surface
  text = cmd[update:0] $SCRIPTS_PATH/truncate.sh "$USER" 16
  text_align = center
  font_size = 20
  font_family = $FONT Bold
  position = 65, 165
  halign = center
  valign = center
  zindex = 3
}

label {
  color = $on_surface
  text = cmd[update:1000] echo "Uptime: $($SCRIPTS_PATH/uptime.sh)"
  text_align = center
  font_size = 12
  font_family = $FONT SemiLight
  position = 65, 140
  halign = center
  valign = center
  zindex = 3
}

# --- 'Session Locked' label --- #

label {
  color = $on_primary
  text = Session Locked
  font_size = 32
  font_family = $FONT Black
  position = 0, 0
  halign = center
  valign = center
  zindex = 3
}

# --- Date & Time labels --- #

label {
  color = $primary
  text = cmd[update:1000] echo "<span font_features='tnum'>$(date +%H)</span>"
  text_align = center
  font_size = 64
  font_family = $CLOCK_FONT Medium
  position = 300, 165
  halign = center
  valign = center
  zindex = 3
}

label {
  color = $primary
  text = cmd[update:1000] echo "<span font_features='tnum'>$(date +%M)</span>"
  text_align = center
  font_size = 64
  font_family = $CLOCK_FONT Medium
  position = 300, 75
  halign = center
  valign = center
  zindex = 3
}

label {
  color = $secondary
  text = cmd[update:1000] echo "<span font_features='tnum'>$(date +%0m)</span>"
  text_align = center
  font_size = 64
  font_family = $CLOCK_FONT Medium
  position = 300, -75
  halign = center
  valign = center
  zindex = 3
}

label {
  color = $secondary
  text = cmd[update:1000] echo "<span font_features='tnum'>$(date +%0d)</span>"
  text_align = center
  font_size = 64
  font_family = $CLOCK_FONT Medium
  position = 300, -165
  halign = center
  valign = center
  zindex = 3
}

# --- Battery icon & labels --- #

label {
  text = cmd[update:60000] $SCRIPTS_PATH/battery.sh icon
  color = $primary
  font_size = 90
  font_family = $MONOSPACE_FONT
  position = 120, -155
  halign = center
  valign = center
  zindex = 3
}

label {
  color = $on_surface
  text = cmd[update:60000] echo "Battery: $($SCRIPTS_PATH/battery.sh percentage)"
  text_align = center
  font_size = 20
  font_family = $FONT Bold
  position = -60, -145
  halign = center
  valign = center
  zindex = 3
}

label {
  color = $on_surface
  text = cmd[update:60000] echo "Status: $($SCRIPTS_PATH/battery.sh status)"
  text_align = center
  font_size = 12
  font_family = $FONT SemiLight
  position = -60, -170
  halign = center
  valign = center
  zindex = 3
}

# --- Input field & container --- #

input-field {
  size = 300, 50
  outline_thickness = 2
  dots_size = 0.3 # Scale of input-field height, 0.2 - 0.8
  dots_spacing = 0.15 # Scale of dots' absolute size, 0.0 - 1.0
  dots_center = true
  dots_rounding = -1 # -1 default circle, -2 follow input-field rounding
  outer_color = $surface_container_high
  inner_color = $surface_container
  font_color = $primary
  font_family = $FONT Bold
  fade_on_empty = false
  fade_timeout = 2000
  placeholder_text = Enter Password... # Text rendered in the input box when it's empty.
  hide_input = false
  rounding = 8 # -1 means complete rounding (circle/oval)
  check_color = $primary
  fail_color = $error # if authentication failed, changes outer_color and fail message color
  fail_text = $FAIL <b>($ATTEMPTS)</b> # can be set to empty
  fail_transition = 300 # transition time in ms between normal outer_color and fail_color
  capslock_color = $surface_container_high
  numlock_color = $surface_container_high
  bothlock_color = -1 # when both locks are active. -1 means don't change outer color (same for above)
  invert_numlock = false # change color if numlock is off
  swap_font_color = false # see below
  position = 0, -375
  halign = center
  valign = center
  zindex = 4
}

shape {
  size = 315, 65
  border_size = 2
  border_color = $surface_variant
  color = $surface
  shadow_passes = 2
  shadow_size = 2
  shadow_color = $shadow
  rounding = 15
  position = 0, -375
  halign = center
  valign = center
  zindex = 3
}

# --- Layout label & boxes --- #

shape {
  size = 200, 25
  border_size = 2
  border_color = $surface_container_high
  color = $surface_container
  rounding = 5
  position = 0, -440
  halign = center
  valign = center
  zindex = 2
}

shape {
  size = 210, 35
  border_size = 2
  border_color = $surface_variant
  color = $surface
  shadow_passes = 2
  shadow_size = 1
  shadow_color = $shadow
  rounding = 10
  position = 0, -440
  halign = center
  valign = center
  zindex = 1
}

label {
  color = $secondary
  text = cmd[] $SCRIPTS_PATH/truncate.sh "$LAYOUT" 15
  text_align = center
  font_size = 11
  font_family = $FONT SemiLight
  position = 0, -440
  halign = center
  valign = center
  zindex = 3
}

# --- Layout buttons --- #

label {
  text = 󰍞
  color = $secondary
  font_size = 25
  font_family = $MONOSPACE_FONT
  position = -90, -440
  halign = center
  valign = center
  zindex = 3
  onclick = niri msg action switch-layout prev
}

label {
  text = 󰍟
  color = $secondary
  font_size = 25
  font_family = $MONOSPACE_FONT
  position = 90, -440
  halign = center
  valign = center
  zindex = 3
  onclick = niri msg action switch-layout next
}

# --- Greeting container & label --- #

shape {
  size = 300, 50
  border_size = 2
  border_color = $surface_container_high
  color = $surface_container
  rounding = 8
  position = 0, 375
  halign = center
  valign = center
  zindex = 3
}

shape {
  size = 315, 65
  border_size = 2
  border_color = $surface_variant
  color = $surface
  shadow_passes = 2
  shadow_size = 2
  shadow_color = $shadow
  rounding = 15
  position = 0, 375
  halign = center
  valign = center
  zindex = 2
}

label {
  color = $primary
  text = cmd[update:60000] $SCRIPTS_PATH/truncate.sh "$(echo "Good $(date +%H | awk '{if ($1 < 12) print "Morning"; else if ($1 < 18) print "Afternoon"; else print "Evening"}')")" 30
  text_align = center
  font_size = 24
  font_family = $FONT Bold
  position = 0, 375
  halign = center
  valign = center
  zindex = 4
}

# --- Media label & boxes --- #

shape {
  size = 200, 25
  border_size = 2
  border_color = $surface_container_high
  color = $surface_container
  rounding = 5
  position = 0, 440
  halign = center
  valign = center
  zindex = 2
}

shape {
  size = 210, 35
  border_size = 2
  border_color = $surface_variant
  color = $surface
  shadow_passes = 2
  shadow_size = 1
  shadow_color = $shadow
  rounding = 10
  position = 0, 440
  halign = center
  valign = center
  zindex = 1
}

label {
  color = $secondary
  text = cmd[update:1000:1] [[ -z "$(playerctl status)" || "$(playerctl status)" = "Stopped" ]] && echo 'No Media Playing'
  text_align = center
  font_size = 12
  font_family = $FONT SemiLight
  position = 0, 440
  halign = center
  valign = center
  zindex = 4
}

label {
  color = $secondary
  text = cmd[update:1000:1] [[ -n "$(playerctl status)" && "$(playerctl status)" != "Stopped" ]] && echo "Media $(playerctl status)"
  text_align = center
  font_size = 12
  font_family = $FONT SemiLight
  position = -35, 440
  halign = center
  valign = center
  zindex = 4
}

# --- Media control buttons --- #

label {
  text = cmd[update:1000:1] [[ -n "$(playerctl status)" && "$(playerctl status)" != "Stopped" ]] && echo "󰼨"
  color = $secondary
  font_size = 15
  font_family = $MONOSPACE_FONT
  position = 40, 440
  halign = center
  valign = center
  zindex = 3
  onclick = playerctl previous && pkill -USR2 hyprlock
}

label {
  text = cmd[update:1000:1] if [ "$(playerctl status)" = "Playing" ]; then echo "󰏤"; elif [ "$(playerctl status)" = "Paused" ]; then echo "󰐊"; fi
  color = $secondary
  font_size = 15
  font_family = $MONOSPACE_FONT
  position = 60, 440
  halign = center
  valign = center
  zindex = 3
  onclick = playerctl play-pause && pkill -USR2 hyprlock
}

label {
  text = cmd[update:1000:1] [[ -n "$(playerctl status)" && "$(playerctl status)" != "Stopped" ]] && echo "󰼧"
  color = $secondary
  font_size = 15
  font_family = $MONOSPACE_FONT
  position = 80, 440
  halign = center
  valign = center
  zindex = 3
  onclick = playerctl next && pkill -USR2 hyprlock
}

# --- Power buttons --- #

label {
  text = 󰐥
  color = $error
  font_size = 80
  font_family = $MONOSPACE_FONT
  position = -300, 155
  halign = center
  valign = center
  zindex = 3
  onclick = systemctl poweroff
}

label {
  text = 󰍃
  color = $secondary
  font_size = 80
  font_family = $MONOSPACE_FONT
  position = -295, 0
  halign = center
  valign = center
  zindex = 3
  onclick = niri msg action quit -s
}

label {
  text = 󰤄
  color = $yellow
  font_size = 80
  font_family = $MONOSPACE_FONT
  position = -300, -155
  halign = center
  valign = center
  zindex = 3
  onclick = systemctl suspend
}
